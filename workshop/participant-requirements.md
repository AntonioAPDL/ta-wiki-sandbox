# Participant Requirements Message

Copy/paste this to participants before the workshop:

---
You will need:
- A laptop.
- A GitHub account.
- Git installed (`git --version` works).
- VS Code installed.
- The workshop repo link (see `README.md` in this repo).

Optional: arrive 10 minutes early for setup issues.
---
